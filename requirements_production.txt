# ============================================================================
# MeStocker API - Production Requirements (Lightweight)
# ============================================================================
#
# Este archivo contiene SOLO las dependencias esenciales para producción:
# - FastAPI + Uvicorn
# - PostgreSQL (async)
# - SQLAlchemy ORM
# - Alembic migrations
# - Authentication (JWT + bcrypt)
# - Pydantic validation
# - Environment config
# - Structured logging
#
# EXCLUIDAS: torch, chromadb, sentence-transformers, transformers y ML/AI libs.
# ChromaDB se agregará cuando sea necesario en producción.
# Redis incluido para caching y sessions.
# ============================================================================

# FastAPI Framework
fastapi==0.116.1
uvicorn[standard]==0.35.0
python-multipart==0.0.6

# PostgreSQL Drivers (async + sync)
asyncpg==0.29.0
psycopg2-binary==2.9.9

# SQLAlchemy ORM (async support)
sqlalchemy==2.0.41

# Database Migrations
alembic==1.13.1

# Pydantic (validation + settings)
pydantic==2.11.7
pydantic-settings==2.2.1
email-validator==2.1.0

# Authentication & Security
python-jose==3.5.0
passlib==1.7.4
bcrypt==4.3.0
cryptography==44.0.0

# Configuration
python-dotenv==1.1.1

# Redis (async support for caching & sessions)
redis>=5.0.0

# Structured Logging
structlog==25.4.0
loguru==0.7.2

# Communications (SMS/WhatsApp/Email)
httpx>=0.28.0  # HTTP client for Bird SMS API
resend>=2.0.0

# Async File I/O
aiofiles>=23.0.0

# Image Processing
pillow>=10.0.0

# Template Engine
jinja2>=3.1.0

# QR Code Generation
qrcode[pil]>=7.4.0

# ============================================================================
# Total: 24 packages (vs ~50+ en requirements.txt completo)
# Tamaño estimado: ~80MB (lightweight deployment)
# Tiempo de instalación: ~50 segundos
# ============================================================================
