// This component has been replaced by CheckoutPage.tsx with modern checkout flow
// Redirect to the new checkout page implementation
import React from 'react';
import CheckoutPage from './CheckoutPage';

const Checkout: React.FC = () => {
  return <CheckoutPage />;
};

export default Checkout;